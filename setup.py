from distutils.core import setup
setup(
  name = 'ssim_api',
  packages = ['ssim_api'], # this must be the same as the name above
  version = '0.0.2',
  description = 'A library for querying a .ssim database',
  author = 'Jason Sherba',
  author_email = 'jsherba@usgs.gov',
  url = 'https://gitlab.com/jsherba/ssim_python_API', # use the URL to the github repo
  download_url = 'https://gitlab.com/jsherba/ssim_python_API/tarball/0.0.2',
  keywords = ['testing', 'logging', 'example'], # arbitrary keywords
  classifiers = [],
)